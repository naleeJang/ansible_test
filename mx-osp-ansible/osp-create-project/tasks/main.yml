---
# tasks file for osp-create-project

# Create a project
- openstack.cloud.project:
    state: present
    name: "{{ project_name }}"
    description: "{{ project_description }}"
    domain_id: default
    enabled: True
